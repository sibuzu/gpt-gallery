這個project在 netlify 會有兩個 site
sites/gallery/   <- 目前的 site 移到這裡
sites/design/   -> 新的 site

images 的是共用的
gallery site 的行為不變

design site 則是只用 Design/ 下的 image
且 catalogs 為 現代、雲紗、雅裁、線舞、元素、民族
現代 -> 現代-*
雲紗 -> 雲紗-*
雅裁 -> 雅裁-*
線舞 -> 線舞-*
元素 -> 元素-*
民族 -> 民族-*

不用 group expand/collapse
不用 shuffle，用檔案順序
